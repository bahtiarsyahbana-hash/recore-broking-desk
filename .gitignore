# macOS
.DS_Store
**/.DS_Store

# Editors
.vscode/
.idea/
*.swp

# Node — the app has no dependencies, but keep these out if any are ever added
node_modules/
npm-debug.log*
*.tsbuildinfo

# Local environment
.env
.env.local

# Reference workbook is unzipped here to regenerate data; only the generated
# JavaScript is committed, so the diff shows what changed in the data.
scripts/import/workbook/
